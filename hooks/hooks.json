{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f .tasks/board.json ]; then bin/board-status; else echo 'board-status: no board.json — run board-pull to populate'; fi"
          }
        ]
      }
    ]
  }
}
